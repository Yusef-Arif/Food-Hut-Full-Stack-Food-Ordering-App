import React from "react";
import ProductForm from "../_components/product-form";

const page = () => {
  return (
    <main>
      <h1 className="text-3xl font-bold text-primary">Add Product</h1>
      <ProductForm />
    </main>
  );
};

export default page;
